{
  "name": "nanocodex-managed-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist --config wrangler.jsonc",
    "check": "npm run typecheck && npm test && npm run build",
    "predeploy": "npm run prepare:code-evaluator",
    "deploy": "wrangler deploy --env=\"\" --config wrangler.jsonc",
    "prebuild": "npm run prepare:code-evaluator",
    "prepare:code-evaluator": "node scripts/prepare-code-evaluator.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/computer": "0.2.1",
    "@cloudflare/sandbox": "0.12.9",
    "@jitl/quickjs-wasmfile-release-asyncify": "0.32.0",
    "accounts": "0.16.2",
    "isomorphic-git": "^1.41.5",
    "nanocodex": "workspace:*",
    "quickjs-emscripten-core": "0.32.0",
    "ox": "0.14.36",
    "viem": "^2.54.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "4.127.1",
    "ws": "^8.18.3"
  }
}
